ParamIntel v0.5.0 verification
Date: 2026-08-28
Status: automated implementation acceptance PASS; Windows Lab A PASS; Windows Lab B PASS; Windows Lab C CLI-side PASS; Lab C server timestamps PENDING

Release goal
------------
v0.5 protects ParamIntel's evidence model from responses that are known to represent rate limiting or explicit server backoff, and adds optional global request-start pacing without changing the discovery algorithms or confidence model.

Core invariant:
- a response known to be rate-limited/backoff must never become a model.Snapshot used by compare.AgainstBaseline

Implementation status
---------------------
Slice 1 — typed backoff classification: COMPLETE
- HTTP 429 always becomes a typed rate_limit BackoffError
- HTTP 503 becomes server_backoff only with a valid Retry-After
- Retry-After integer seconds supported
- Retry-After HTTP-date supported
- malformed Retry-After on 429 is retained as raw metadata but is not trusted as parsed timing
- ordinary 503 remains an ordinary response
- ordinary 403 remains an ordinary response
- classified responses are rejected before Snapshot construction
- rejected response bodies are still closed
- baseline construction aborts immediately if any sample is classified as backoff
- errors.As can recover BackoffError through wrapped baseline errors

Slice 2 — global request-start pacing: COMPLETE
- added -delay duration flag
- default -delay 0 preserves previous unpaced behavior
- negative delay rejected before network activity
- pacing implemented once at the shared HTTP transport
- the same paced client is used by baseline construction and the discovery engine
- spacing is measured between request starts, not as unconditional post-response sleep
- context cancellation interrupts a pacing wait
- pacing state is race-safe
- zero delay returns the underlying transport unchanged

Slice 3 — discovery-phase evidence integrity: COMPLETE
- group probe 429 aborts scan; no narrowing decision is made from limiter behavior
- candidate probe 429 aborts verification before control is sent
- random-name control 429 invalidates the paired experiment; no partial confidence/trial result is returned
- value-aware candidate screen 429 aborts rescue with no finding
- value-aware same-value control 429 aborts rescue with no finding
- semantic request budget still counts requests that were actually sent before the 429
- characterization 429 after a valid discovery aborts the scan rather than returning a partially characterized normal result
- real CLI subprocess regression proves rate-limit abort exits non-zero and does not create the requested findings file

Slice 4 — release hardening: IN PROGRESS
- CLI version bumped to ParamIntel v0.5.0
- release version is pinned by an automated test
- README updated for the v0.5 evidence-integrity model
- CHANGELOG updated for v0.5.0
- reproducible Windows acceptance lab added under labs/v0.5-rate-limit
- Windows Lab A externally accepted
- Windows Lab B externally accepted
- Windows Lab C CLI behavior externally accepted; server timestamp spacing still pending review

Automated gates
---------------
Exact release-hardening repository head before external-result documentation:
  32f15d3f0e024dbe158033cc3ce8a578b8bbb6fb

GitHub Actions run #93:
- go test ./...: PASS
- go vet ./...: PASS
- go test -race ./...: PASS
- Linux build: PASS
- Windows amd64 cross-build: PASS

Earlier exact heads also passed the same full CI gates after Slice 1, Slice 2, and Slice 3.

Backoff classification acceptance
---------------------------------
Automated tests prove:

1. 429 never becomes a Snapshot.
2. BackoffError is discoverable through errors.As.
3. Retry-After seconds parse correctly.
4. Retry-After HTTP-date parses correctly.
5. malformed Retry-After on 429 does not panic and remains raw metadata.
6. 503 + valid Retry-After becomes server_backoff.
7. 503 without Retry-After remains an ordinary response.
8. 503 + malformed Retry-After remains an ordinary response.
9. 403 remains an ordinary response.
10. rejected 429 response bodies are closed.
11. a baseline that receives 429 fails immediately and does not accept remaining samples.

Discovery integrity acceptance
------------------------------
Automated tests prove:

- group probe -> 429
  result: Scan returns BackoffError and no normal results

- candidate -> 429
  result: control is not sent and no confidence/trials are returned

- candidate -> normal behavioral change; random control -> 429
  result: pair is invalid, scan errors, no partial confidence is produced

- value-aware screen -> 429
  result: rescue errors, no result/value is returned, one actually-sent request is charged to semantic budget

- value-aware candidate screen -> meaningful; same-value control -> 429
  result: rescue errors, no finding, two actually-sent requests are charged to semantic budget

- confirmed candidate -> characterization -> 429
  result: Scan aborts and does not return a normal partially characterized result

CLI report-suppression acceptance
---------------------------------
Automated subprocess test runs the real CLI against a local 429 server with an -output path.

Required and observed automated behavior:
- CLI exits non-zero
- diagnostic contains "rate limit detected"
- diagnostic contains "response was not used as discovery evidence"
- requested findings file is not created

Pacing acceptance
-----------------
Automated tests prove:
- -delay 0 adds no pacing wrapper
- positive delay enforces minimum request-start spacing
- elapsed time from a slow previous response can satisfy the configured start interval, avoiding unnecessary additional delay
- waiting is context-cancellable
- concurrent transport use passes go test -race
- negative CLI delay is rejected

External Windows CLI acceptance
-------------------------------
Lab A — mid-scan 429
Status: PASS
Observed Windows CLI behavior:
- baseline: 3 samples
- baseline status: 200 stable
- baseline body length: 19-19 bytes
- stable JSON paths: 4
- active location: query
- total candidate placements: 26 in 1 initial group
- active group probe aborted with:
  probe group query:account_id: rate limit detected: HTTP 429 (Retry-After: 2); response was not used as discovery evidence
- process exit code: 1
- Test-Path v05-mid-scan-findings.json returned False

Conclusion:
- baseline evidence remained valid before rate limiting began
- the first classified mid-scan 429 aborted group probing rather than becoming narrowing evidence
- no normal findings report was emitted after evidence integrity was lost

Lab B — asymmetric candidate/control limiter
Status: PASS
Observed Windows CLI behavior:
- context intelligence observed 4 JSON properties
- actionable response-only candidates: 1
- skipped already-present properties: 3
- skipped candidates with missing request parent: 0
- baseline: 3 samples
- baseline status: 200 stable
- baseline body length: 22-22 bytes
- stable JSON paths: 3
- active location: json
- prioritized contextual candidate placements: 1
- total candidate placements: 52 in 3 initial groups
- scan aborted with:
  rate limit detected: HTTP 429 (Retry-After: 2); response was not used as discovery evidence
- process exit code: 1
- requested v05-asymmetric-findings.json did not exist after the run

Conclusion:
- an asymmetric limiter response invalidated the experiment instead of producing a confidence score or findings report
- the real Windows CLI preserved the fail-closed evidence-integrity boundary

Lab C — pacing
Status: CLI-SIDE PASS; SERVER TIMESTAMP REVIEW PENDING
Observed Windows CLI behavior:
- baseline: 3 samples
- baseline status: 200 stable
- baseline body length: 11-11 bytes
- stable JSON paths: 2
- CLI reported: Request pacing: minimum 100ms between request starts
- active location: query
- total candidate placements: 26 in 1 initial group
- scan completed normally with 0 parameters
- requested v05-pace-findings.json was written
- process exit code: 0
- Test-Path v05-pace-findings.json returned True

Remaining Lab C criterion:
- review the lab-server [pace] request-start lines and confirm consecutive starts are approximately >=100ms rather than back-to-back

Do not mark full Windows external acceptance PASS until the Lab C server timing lines have been reviewed.

Preserved v0.4 regression model
-------------------------------
v0.5 does not change candidate acquisition, mutation semantics, confidence scoring, or value-aware rescue logic.

Existing automated coverage retained includes:
- debug=true value-aware discovery
- paired same-value random-name false-positive rejection
- insufficient semantic-budget refusal
- deterministic semantic budget ordering
- typed JSON boolean/integer semantic probes
- context + typed boolean $.options.include_deleted composition
- related-response candidate provenance
- GraphQL-style seeded query verification
- form-body discovery
- root/nested JSON discovery
- nonstandard JSON MIME handling
- contextual chosen_discount mass-assignment regression
- negative-control rejection
- entropy failure remains fail-closed

Historical v0.4 external acceptance
-----------------------------------
Value-sensitive query case:
- same controlled debug endpoint returned 0 parameters in v0.3
- v0.4 discovered debug using discovery_value=true
- discovery_value_kind=string
- candidate changed 3/3
- random-name same-value control changed 0/3
- confidence 1.00 HIGH
- -characterize=false confirmed discovery is independent of characterization

Context + typed JSON case:
- related response contributed $.options.include_deleted with observed_type=boolean
- v0.4 discovered it using typed JSON boolean true
- candidate changed 3/3
- random control changed 0/3
- confidence 1.00 HIGH
- candidate_sources and discovery provenance were both preserved

Known boundaries
----------------
v0.5 deliberately does not add:
- automatic retries after 429/503
- automatic sleeping/resume from Retry-After
- adaptive concurrency
- rate-limit/WAF inference from arbitrary 403 pages or body text
- cache-aware probe correlation
- server-side parameter-pollution mutation inside another parameter value
- missing-parent JSON scaffolding synthesis
- array insertion
- AI-generated semantic values
- Burp/MCP integration
- broad business-state enum spraying

ParamIntel findings remain research leads requiring manual authorization, impact, and exploitability validation.
